#ifndef TYPES_H
#define TYPES_H

// im so tired of typing unsigned char
#include <stdint.h>

typedef uint8_t  u8;
typedef uint16_t u16;
typedef uint32_t u32;
typedef uint64_t u64;
typedef int8_t   s8;
typedef int16_t  s16;
typedef int32_t  s32;

// i originally had these too but i never ended up using them
// keeping them just in case
// typedef unsigned char byte;
// typedef unsigned short word;

#include <stdbool.h>

#endif